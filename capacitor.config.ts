import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cs-demo-secure-storage-react',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
